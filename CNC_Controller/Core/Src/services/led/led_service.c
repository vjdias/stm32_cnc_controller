#include "services/led/led_service.h"

void led_service_init(void) { /* implementar controle de GPIO se necessário */
}
void led_on_led_ctrl(const uint8_t *frame, uint32_t len) {
	(void) frame;
	(void) len; /* parsear e aplicar quando integrar */
}
